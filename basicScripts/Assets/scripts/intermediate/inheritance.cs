using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class inheritance : MonoBehaviour
{
    // inheritance related access modifier : 1) private 2) public 
    // 3) protected(hybrid of private and public. parent/child class available, not other class)
    // Start is called before the first frame update
    // note that inheritance is hierarchical
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
